{
  "name": "incantation",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.3.5",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "setup": "bun install && bun run check:system && uv sync --directory apps/vision-service && bun run build",
    "check:system": "bun scripts/check-system-deps.ts",
    "setup:leap": "bun run setup && bun scripts/setup-leap.ts",
    "setup:leap:bindings": "bun scripts/setup-leap.ts --skip-system",
    "check:leap": "bun scripts/setup-leap.ts --verify-only",
    "dev": "bun run check:system && bun scripts/dev.ts",
    "build": "bun run --cwd packages/shared build && bun run --cwd apps/desktop build",
    "test": "bun test apps/desktop/tests/main && uv run --directory apps/vision-service pytest",
    "test:leap": "uv run --directory apps/vision-service python tools/leap_smoke_test.py",
    "check": "bunx biome check . && bun test apps/desktop/tests/main && uv run --directory apps/vision-service ruff check . && uv run --directory apps/vision-service ty check",
    "test:report": "bun scripts/test-report.ts",
    "report:open": "bun scripts/report-html.ts --open",
    "test:report:open": "bun run test:report && bun run report:open",
    "test:smoke:x11": "xvfb-run -a bun scripts/x11-smoke.ts",
    "test:smoke:pipeline": "xvfb-run -a bun scripts/pipeline-smoke.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "chokidar": "^5.0.0"
  }
}
